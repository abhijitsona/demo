pipeline {
    agent {
        docker { image 'alpine:latest' }
    }
    stages {
        stage('build') {
            steps {
                sh 'alpine --version'
                sh 'echo Hello Jenkins'
            }
        }
    }
}
